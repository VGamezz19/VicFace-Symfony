{% extends '::base.html.twig' %}

{% block body %}
    <h1>Articles CRUD</h1>

    <div class = "row">
    {% for articulo in articulos %}

        <div class ="col-sm-4 col-md-4 col-lg-4">
        <div class="card">

            <img class="card-img-top" src="..." alt="Card image cap">
            <div class="card-block">
                <h4 class="card-title">{{ articulo.titulo }}</h4>
                {% if articulo.user == app.user%}
                    <span >
                        <input type="button" href = "/delete" class ="glyphicon glyphicon-remove">
                    </span>
                {% endif %}
                <p class="card-text">{{ articulo.user }}</p>
                <p> {{ articulo.cuerpo }}</p>
            </div>
        </div>
        </div>

    {% endfor %}
    </div>

{% endblock %}